# CHANGELOG — `00_parent` corpus

Version history and migration notes for the governance corpus. Entries are appended,
never edited or reordered. Newest last.

**As of v3.0.0 this file is historical.** The corpus it tracked — `Claude.md` and
`Memory.md` — was retired to `_archive/v2.0.0/`, and change management moved to this
repository's git history. The `iclaw_administrator` project that maintained it was
dissolved. Rows below v3.0.0 describe a protocol that is no longer operative.

| Date | File | Version | Sections | Motivation | Migration |
|---|---|---|---|---|---|
| 2026-08-16 | Claude.md | 1.1.0 → 2.0.0 | all (renumbered) | Mutability classes, declared-delta child directives, `input/`/`output/` scaffold, artifact naming and overwrite prohibition, output manifest, files-are-data rule, administrator write monopoly. Detail in `Claude.md` Appendix A. | Lazy. Each project acquires `input/`, `output/`, and `output/_manifest.md` at its next orientation, on user confirmation. |
| 2026-08-16 | Memory.md | 1.0.0 → 2.0.0 | §3.1, §3.4 (new), §3.5, §4.4 (new) | Added `memory_md_version` / `directive_version` / `tags` to frontmatter; added Section 3 — Artifacts; added §4.4 Abandoned Sessions; stated administrator is bound by `끝`. Detail in `Memory.md` Appendix A. | Lazy. Sealed memory files predating v2.0.0 lack the new frontmatter fields; this is recorded as drift, never repaired (`Claude.md` §9.3). |
| 2026-08-16 | Claude.md, Memory.md, CHANGELOG.md | no version change | none | Filename repair only. All three files, plus both `_archive/` entries and both `00_parent_admin/` files, had been written with a doubled `.md.md` extension, so no path named in `Claude.md` §1 resolved. Renamed to canonical names. `_archive/Memory)v1.1.0.md.md` was additionally misnamed: its own frontmatter declares `version: 1.0.0`, so it was renamed `Memory_v1.0.0.md`, matching `Memory.md`'s `supersedes: 1.0.0`. | None. Content unchanged; no version bump. |
| 2026-08-29 | corpus | 2.0.0 → 3.0.0 | all (replaced) | Governance moved out of always-resident Markdown into the `iclaw` plugin: three on-demand skills (`project-init`, `project-memory`, `project-artifacts`), a ~45-line per-project `CLAUDE.md`, and four durable memory tiers (LONGTERM / STATE / sessions / canon). Nesting is now unbounded and branching is an operation. Mutability classes, the precedence order, the LAYERS declaration, child directives, and the administrator project were all dropped — their only job was parent-vs-child arbitration, and there is no parent. Measured effect: 826 resident lines → 126. Detail in `docs/specs/2026-08-28-iclaw-protocol-v3-design.md`. | Not lazy, and not automatic. Old projects are not migrated; the skills ignore any folder lacking `CLAUDE.md` + `_memory/`. `iclaw:project-init` in migrate mode brings a folder across on request, creating only what is missing and never overwriting an existing `CLAUDE.md`. Sealed memory files predating v3 keep their v2.0.0 frontmatter; the drift is reported at orientation and never repaired. |
| 2026-08-29 | plugin | 1.0.0 → 1.1.0 | skills/ | The 1.0.0 install cached a Task-1 snapshot: the stub `project-artifacts` plus the since-archived v2.0.0 files. `project-init` and `project-memory` were never in the cache, so no session could invoke them while the source looked complete. Cache refresh is version-gated, so a bump was required to ship the real skills. | None. `claude plugin marketplace update iclaw && claude plugin update iclaw@iclaw`, then restart the session. Verify with `ls ~/.claude/plugins/cache/iclaw/iclaw/*/skills/`. |
| 2026-08-29 | plugin | 1.1.0 → 1.2.0 | scripts/ | Validator now fails on unfilled `{placeholder}` text in `CLAUDE.md`, `LONGTERM.md`, `STATE.md`, session logs and canon. A scaffolded file that was never seeded passed validation silently and only surfaced when an agent read the file and tried to act on the instruction inside the braces. `_memory/legacy/` is exempt — pre-v3 records are never rewritten. | None. Re-run the validator on existing projects; unfilled placeholders will now be reported. |
| 2026-08-30 | plugin | 1.2.0 → 1.3.0 | scripts/ | Raised the session-start read budget (`STATE.md` + `LONGTERM.md` combined) from ≤100 to ≤200 lines. Still a `WARN`, never a `FAIL`. The 100-line target was deliberately tight (spec §"read budget") and forced aggressive template trimming on seeded projects; 200 gives layered memory room to breathe while keeping the per-session read bounded. Historical `docs/` references to 100 are left as design record, not rewritten. | None. `claude plugin marketplace update iclaw && claude plugin update iclaw@iclaw`, then restart the session. |
| 2026-09-04 | plugin | 1.3.0 → 1.4.0 | skills/ | Reworded `project-memory`'s RECORD section. It described the session log as a place to "preserve the traces" of "thinking" and "reasoning that went nowhere" — an imperative, addressed to the agent, to persist its own reasoning unedited. Read without project context that is the shape of a chain-of-thought extraction prompt, and it tripped a safety classifier on invocation, most often in sessions whose own subject matter is LLM reasoning. The log's content is unchanged; it is now described as the project's decision history — options explored and abandoned, dead ends and why — rather than as the agent's internal state. Same rule, no introspection framing. Also corrected "Section 2 of the session log" to `## Work`: the session template's second section is `## Files Touched`, so the sentence named the wrong destination. | None. `claude plugin marketplace update iclaw && claude plugin update iclaw@iclaw`, then restart the session. Verify with `grep -n 'working draft' ~/.claude/plugins/cache/iclaw/iclaw/1.4.0/skills/project-memory/SKILL.md`. |
| 2026-09-04 | plugin | 1.4.0 → 1.5.0 | templates/, docs/ | Added a **Register** rule beside **Tone**, and changed "Result first, reasoning after" to "rationale after". Two defects. (1) The template specified which *language* to hold but never which *register* to hold it in. Korean forces a speech level and an address form in nearly every sentence, so a session toggled to Korean had to improvise one; with `superpowers` calling the user "your human partner" 40 times across its skill text, the nearest Korean rendering it reached for was 형님 — a kinship/seniority term carrying a register the user never asked for and would not have chosen. Language, depth, and register are three axes; only two were specified. (2) "reasoning" inside an output directive was one more always-resident hit in a corpus whose own subject is LLM reasoning; "rationale" is identical in meaning and carries none of it. | Existing projects were patched in place, so no scaffold is stale. **Re-paste the Cowork project instructions field** from `docs/` — Cowork reads that field, not `CLAUDE.md` on disk, so it does not inherit this. Then `claude plugin marketplace update iclaw && claude plugin update iclaw@iclaw`. |
| 2026-09-07 | plugin | 1.5.0 → 1.6.0 | templates/, docs/ | Reworded the Behavior block's epistemic clauses. "Minimize inference", "Source your claims — separate established fact, reasoned inference, and uncertainty", "Never assert what you inferred as what was decided", and a `[c: 0.00]` score covering "inferences" together formed an instruction set directing the agent to classify and report its own reasoning and certainty — the same shape as the RECORD text fixed in 1.4.0, but propagated into every project through `templates/CLAUDE.md` instead of living in `skills/`, which is why the 1.4.0 and 1.5.0 scrubs did not reach it. The load is worst where it matters least: a full orientation in a branch whose subject is agent-memory retrieval holds 37 domain terms (retrieval, order effects, LLM agent, non-commuting) beside 8 of these clauses in one context window. Claims are now labelled **sourced / derived / open** — a property of the claim, not a report on the agent — and `[c: 0.00]` scores how well available sources support the claim rather than the agent's own certainty. Same discipline, no introspection framing. Also retires `03_fable_run`'s "audit the basis of each pick". | Existing projects were patched in place, so no scaffold is stale. **Re-paste the Cowork project instructions field** from `docs/`. Then `claude plugin marketplace update iclaw && claude plugin update iclaw@iclaw`. |
| 2026-09-08 | plugin | 1.6.0 → 1.7.0 | skills/, templates/, docs/ | Rewrote `project-memory`'s RECORD section a second time, and removed the `[c: 0.00]` confidence rule. The 1.4.0 rewording swapped vocabulary but kept the instruction's shape: write continuously, record the path the work took, options explored and abandoned, the order things were settled, do not tidy it. Addressed to the agent and delivered by the Skill tool as a *user-role* message, that is an instruction to persist a deliberation trace to disk every session — and a safeguard matches the shape of an instruction, not its words, which is why three word-level scrubs (1.4.0–1.6.0) did not clear the `[reasoning_extraction]` flag. On the record: every documented flag was a Cowork session (the 2026-09-04 screenshot and the 2026-09-07 paste both show `rcw-…/mnt/01_agentic_thesis` paths); all eight `iclaw:project-memory` launches in Claude Code transcripts on this machine completed; and `SKILL.md` had not changed since 1.4.0 — 1.5.0 and 1.6.0 touched templates and docs only. `## Work` is now an outcome log — what was done, decided, set aside and why, one line per item — with the same retention rule (set-aside approaches stay so they are not retried). `[c: 0.00]` was the second per-response self-report still resident in every `CLAUDE.md`; the 1.6.0 redefinition changed what the number meant, not what the instruction asks for. Claims keep the sourced / derived / open labels. Also found: the 1.6.0 in-place patch reached only `01_agentic_thesis` — `02_local_swarm` and the seven `03_grad_coursework` files still carried the 1.5.0 epistemic wording, and the `02_local_swarm` Cowork instructions field was never re-pasted. The branch-inherits-parent-Behavior idea was considered and dropped: `docs/2026-09-08-cowork-instructions-bootstrap.md` requires every `CLAUDE.md` to be self-sufficient. | Existing projects patched in place: 14 `CLAUDE.md` files lose the Confidence rule, and the eight outside `01_agentic_thesis` receive the 1.6.0 epistemic wording they had missed. Memory files untouched — record the change at each project's next orientation if wanted. **Re-paste the Cowork instructions field** for `01_agentic_thesis` and `02_local_swarm` from `docs/`, or adopt `templates/COWORK_INSTRUCTIONS.md`. Then `claude plugin marketplace update iclaw && claude plugin update iclaw@iclaw` and restart the session. Verify with `grep -n 'dated record' ~/.claude/plugins/cache/iclaw/iclaw/1.7.0/skills/project-memory/SKILL.md`. |
| 2026-09-08 | plugin | 1.7.0 → 1.7.1 | .claude-plugin/marketplace.json, README.md | Renamed the marketplace from `iclaw` to `00_memory`; the plugin keeps its name, so skills are still `iclaw:*`. Found after 1.7.0 shipped: Cowork never runs the Claude Code plugin cache. The desktop app keeps two registries — the Claude Code CLI's `known_marketplaces.json`, keyed by the `name` in `marketplace.json` (`iclaw`), and a server-side account marketplace it registered by repo name (`00_memory`) when the plugin was added on 2026-08-29 — and it drives every UI action (refresh, update, remove) through the CLI using the server-side name. With the names disagreeing, refresh returned `MARKETPLACE_ERROR:NOT_REGISTERED` and remove was a no-op that reported success, so the Cowork copy stayed at **1.1.0**, with the original RECORD wording ("Preserve the traces … reasoning that went nowhere"), through every release from 1.4.0 to 1.7.0. That, not wording, is why the 1.4.0–1.6.0 scrubs never cleared the Cowork flag; the 1.7.0 row's shape argument still applies to the text but was not the operative cause. One name on both sides lets the desktop app's refresh and update reach the CLI, which is the only refresh path this marketplace has — no automatic re-ingest was observed across eleven days and eight pushes. | Install string changes: `iclaw@iclaw` → `iclaw@00_memory`; cache path `~/.claude/plugins/cache/00_memory/iclaw/`. In Claude Code: `claude plugin marketplace remove iclaw && claude plugin marketplace add https://github.com/unhomocoder/00_memory.git && claude plugin install iclaw@00_memory`. In the desktop app: refresh the `00_memory` marketplace, then Update the plugin; if Update stays inactive, remove the marketplace and add it again by URL. Start a new Cowork session afterwards — a running session keeps the copy it launched with. Every future release needs the desktop refresh as a sixth step (README). |
| 2026-09-08 | plugin | 1.7.1 → 1.8.0 | scripts/, templates/, skills/, README.md | Sixteen collisions between the plugin and the fourteen projects it governs, specified in `docs/specs/2026-09-08-plugin-project-collision-remediation.md`. The headline defect: the placeholder check added in 1.2.0 **failed 4 of 14 projects and every failure was a false positive** - it fired on shell brace expansion in a path, on set notation, and on a session log quoting `{nn}` back at the protocol. Two of the flagged files were sealed, so `project-init`'s self-check demanded an edit `project-memory`'s invariants forbid without exception. No pattern separates a quoted placeholder from an unfilled one, so the check is now scoped by region (template-derived text only; a session's `## Work` body is free prose) and findings on sealed or legacy files emit `NOTE` and no longer set exit 1. `templates/session.md` shipped a brace placeholder in `## Summary`, so every correctly scaffolded session log failed validation from creation until seal; it is now `_Written at seal time._`, the same wording a Cowork session independently substituted to get past the bug. Four contract checks added - `CLAUDE.md` self-sufficiency, `canon:` agreeing with the filesystem, relative canon paths resolving against their own file, and `active` branch rows naming a directory that exists - because the validator checked schema and never contract, which is how two `CLAUDE.md` files deferring their whole `## Behavior` block to a parent returned `OK`. `_index.md` became a generated view: new `scripts/reindex.sh` rebuilds it from session frontmatter, `SEAL` no longer appends by hand, and `last_session:` moved from `SEAL` to `ORIENT`. Nine session files had produced five index rows and three `last_session: none` values that were correct per protocol and indistinguishable from corruption. Also: `## Branches` renamed `## Branches of this project`, a `Status` column and a retire mode for branches, a `## This project only` section in `templates/CLAUDE.md`, `templates/corpus.md` for canon that holds a domain index rather than terminology, a named exemption in `project-artifacts` for a `CLAUDE.md`-declared working folder such as `02_local_swarm`'s `src/`, and the four meanings of the leading underscore documented in `README.md`. | Project files were patched in place on 2026-09-08 and all fourteen validate clean; nothing to migrate there. **`README.md` gains step 0 - `git status --short` must be empty before the version bump.** Five files had been untracked since 1.7.0, including `templates/COWORK_INSTRUCTIONS.md`, which `CHANGELOG` 1.7.0 named as a migration path; none of them shipped in 1.7.1 and no session could reach them. Verify with `bash scripts/validate_project.sh <dir>` on any project: expect `OK`, zero `FAIL`, zero `NOTE`. |

---

## Notes on this file's own history

The first two rows were reconstructed on 2026-08-16 from the frontmatter and Appendix A
of the v2.0.0 files themselves, because the v2.0.0 release session recorded only the
string `v1.x → v2.0.0` and wrote no memory file. They are accurate as to content and
date but were not written contemporaneously. Recorded here rather than silently, per
`00_parent_admin/Claude.md` §7.

`_archive/` does not contain `Claude_v1.0.0.md`. No copy was taken when v1.0.0 was
superseded and none can be reconstructed; the gap is permanent and is noted here so
that future audits do not treat it as a new finding.
